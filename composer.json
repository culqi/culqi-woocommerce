{
    "require": {
        "phpseclib/phpseclib": "^3.0",
        "firebase/php-jwt": "6.10.0"
    },
    "require-dev": {
        "php-stubs/wordpress-stubs": "^6.9"
    },
    "scripts": {
        "build": [
            "@install-prod",
            "@package"
        ],
        "install-prod": "composer install --no-dev --optimize-autoloader --no-interaction",
        "package": "bash build/build.sh"
    }
}
